const answer = 'B';

switch(answer) {
  case 'A':
    console.log(`스위치는 ${answer}입니다.`);
    break;
  case 'B':
    console.log(`스위치는 ${answer}입니다.`);
    break;
  case 'C':
    console.log(`스위치는 ${answer}입니다.`);
    break;
  default:
    console.log('스위치는 A, B, C가 아닙니다. 다시 입력해주세요.');
    break;
}